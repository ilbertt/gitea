The static git built by `make nibrun-git` is written to this directory as
`git.gz`. It is a build artifact and is not committed, so this file keeps the
embed directive compilable in a tree that has never run that target.
